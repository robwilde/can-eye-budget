When adding or viewing a transaction, the category selector has a search and dropdown selector with issues.

- the search is not working
- the dropdown is just a list of categories not correctly displaying the hierarchy

## Current Selector

The current selector should have the dropdown removed and the "category / subcategory" should updated to search the list of categories.
`docs/issues/current-category-selector.png`

## Suggested Selector

Here is the suggested category list display. You can see the hierarchy is correct, with categories and subcategories being shown with the `/` separator.
As you type the list is filtered
`docs/issues/suggested-category-selector.png`

## Task

- remove the dropdown selector
- update the search to filter the list of categories as I type
